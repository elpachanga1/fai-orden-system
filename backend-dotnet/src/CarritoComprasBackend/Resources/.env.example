# Copiá este archivo a .env y completá con tus valores locales:
#   cp .env.example .env
#
# Este archivo es usado por seed.sh para conectarse a PostgreSQL.
# NUNCA commitees el archivo .env real.

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=carrito_compras
